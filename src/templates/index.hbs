{{> header}}


    <main role="main" class="bg-light">

      <!-- Main jumbotron for a primary marketing message or call to action -->
      <div class="jumbotron">
        <div class="container">
          <h1 class="display-3">{{data.site.name}}</h1>
          <p>{{data.site.desc}}</p>
          <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more &raquo;</a></p>
        </div>
      </div>

      <div class="container">
        {{{ contents }}}
      </div> <!-- /container -->

    </main>

{{> footer}}
